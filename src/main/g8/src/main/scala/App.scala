package $organization$.$name;format="lower,word"$

import processing.core._

object App {

  def main(args: Array[String]) {
    PApplet.main(
      Array(classOf[$organization$.$name;format="lower,word"$.$name;format="Camel,word"$].getName)
    )
  }

}

